# TODO: CursorIgnore settings are preventing Cursor seeing this when it's named .env.sample as it should be.

# EternalMint Environment Configuration
# Copy this file to .env.local and fill in the values

# ====================
# REQUIRED SECRETS
# ====================

# WalletConnect Project ID (from https://cloud.walletconnect.com/)
NEXT_PUBLIC_PROJECT_ID=your_walletconnect_project_id_here

# Auto Drive API Key (server-side only, for file storage)
AUTO_DRIVE_API_KEY=your_auto_drive_api_key_here

# ====================
# ENVIRONMENT CONTROL
# ====================

# Explicitly set the environment (recommended for production deployments)
# If not set, defaults to 'staging' for production builds, 'development' for dev builds
# NEXT_PUBLIC_ENVIRONMENT=production  # Use this for your production deployment
# NEXT_PUBLIC_ENVIRONMENT=staging     # Use this for staging/preview deployments
# NEXT_PUBLIC_ENVIRONMENT=development # Automatically set in dev mode

# ====================
# OPTIONAL OVERRIDES
# ====================

# Override host URL (only needed for custom domains or special cases)
# NEXT_PUBLIC_HOST=https://your-custom-domain.com

# ====================
# ANALYTICS (OPTIONAL)
# ====================

# Google Analytics ID
# NEXT_PUBLIC_GOOGLE_ANALYTICS_ID=G-XXXXXXXXXX

# ====================
# DEPLOYMENT GUIDE
# ====================
#
# For Vercel deployments:
#
# 1. STAGING (preview deployments, feature branches):
#    - Set NEXT_PUBLIC_PROJECT_ID and AUTO_DRIVE_API_KEY
#    - Don't set NEXT_PUBLIC_ENVIRONMENT (defaults to staging)
#    - Uses staging configuration automatically
#
# 2. PRODUCTION (main branch, custom domain):
#    - Set NEXT_PUBLIC_PROJECT_ID and AUTO_DRIVE_API_KEY
#    - Set NEXT_PUBLIC_ENVIRONMENT=production
#    - Optionally set NEXT_PUBLIC_HOST if using custom domain
#    - Uses production configuration automatically
