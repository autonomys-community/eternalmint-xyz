# TODO: CursorIgnore settings are preventing Cursor seeing this when it's named .env.sample as it should be.

# Sensitive data: API keys, private keys, secrets
NEXT_PUBLIC_PROJECT_ID=
AUTO_DRIVE_API_KEY=""
PRIVATE_KEY=
NEXT_PUBLIC_GOOGLE_ANALYTICS_ID="G-"

# Environment-specific values: URLs, endpoints that change between dev/staging/prod
NEXT_PUBLIC_HOST="http://localhost:3006"

# EVM Network Configuration (for blockchain operations)
NEXT_PUBLIC_EVM_NETWORK="taurus"
NEXT_PUBLIC_CONTRACT_ADDRESS="0x09e8798DAb58C211183c42325Ad7CCd935C11f7D"
NEXT_PUBLIC_RPC_ENDPOINT="https://auto-evm.taurus.autonomys.xyz/ws"
NEXT_PUBLIC_SUBGRAPH_API="https://api.studio.thegraph.com/query/114204/eternalmint-dev/v0.0.16"

# Storage Network Configuration (for file storage - can be taurus or mainnet)
NEXT_PUBLIC_STORAGE_NETWORK="taurus"

# App Configuration
NEXT_PUBLIC_MAX_IMAGE_SIZE_MB=5

# Runtime configuration: Feature flags, debug modes
